{
	"name": "scheduler",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:frontend": "bun run --if-present --cwd apps/frontend dev",
		"dev:api": "bun run --cwd apps/api dev",
		"build": "bun run --if-present --cwd apps/frontend build && bun run --cwd apps/api build",
		"deploy:frontend": "bun run --if-present --cwd apps/frontend deploy",
		"deploy:api": "bun run --cwd apps/api deploy",
		"deploy": "bun run deploy:frontend && bun run deploy:api",
		"typecheck": "bun run --if-present --cwd apps/frontend typecheck && bun run --cwd apps/api typecheck && bun run --cwd packages/shared typecheck",
		"lint": "biome check .",
		"format": "biome format --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"typescript": "^5.5.2"
	}
}
